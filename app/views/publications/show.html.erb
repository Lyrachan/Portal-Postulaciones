<p style="color: green"><%= notice %></p>

<%= render @publication %>

<% if user_signed_in? && current_user.publications.present? == false %>
  <%= button_to "Postular", new_user_postulation_path(publication_id: @publication.id), method: :post, class: "btn btn-info text-white mb-2" %>
<% else %>
  <h5>Ya has postulado a esta publicación</h5>  
<% end %>


<div>
  <%= link_to "Edit this publication", edit_publication_path(@publication) %> |
  <%= link_to "Back to publications", publications_path %>

  <%= button_to "Destroy this publication", @publication, method: :delete %>
</div>
